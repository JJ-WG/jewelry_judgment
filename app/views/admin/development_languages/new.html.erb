# encoding: utf-8

<% render_header -%>
<%- content_for(:header) do -%>
  <%= stylesheet_link_tag 'development_languages' %>
<%- end -%>

    <nav id="subNav">
      <div class="inner">
        <ul id="localNav">
          <li>
            <%= link_to "#{t('web-app-theme.list', :default => 'List')}", admin_development_languages_path %>
          </li>
          <li class="current">
            <%= link_to "#{t('web-app-theme.new', :default => 'New')}", new_admin_development_language_path %>
          </li>
        </ul>
      </div><!-- /.inner -->
    </nav><!-- /#subNav -->
    
    <% render_menu -%>
    
    <section id="content">
      <div class="inner">    
        <header class="pageHeader">
          <h1><%= t('common_label.new_model', :model => DevelopmentLanguage.model_name.human) %></h1>
        </header>
        
        <%= form_for :development_language, :url => admin_development_languages_path, :html => { :class => :form } do |f| -%>
          <% unless flash.blank? -%>
            <div class="errorMsg">
              <% flash.each do |type, message| -%>
                <% if message.is_a?(Enumerable) && message.any? -%>
                  <ul>               
                  <% message.each do |msg| -%>
                    <li><%= msg %></li>
                  <% end -%>
                  </ul>
                <% else -%>               
                  <p><%= message %></p>
                <% end -%>                
                <% if @development_language.errors.any? -%>
                  <ul>
                  <% @development_language.errors.full_messages.each do |msg| -%>
                    <li><%= msg %></li>
                  <% end %>
                  </ul>
                <% end %>
              <% end -%>
            </div>
          <% end %>
          
          <div class="entryForm">
            <%= render :partial => 'form', :locals => {:f => f} %>
          </div><!-- /.entryForm -->
        <% end -%>
      </div><!-- /.inner -->
    </section><!-- /#content -->
