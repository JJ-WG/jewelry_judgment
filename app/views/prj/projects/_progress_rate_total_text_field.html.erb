# encoding: utf-8

                          <%= fields_for object do |f| %>
                            <input type="text" id="project_progress_rate_total"
                                name="project[progress_rate_total]" class="hour"
                                value="<%= @progress_rate_total.to_f.round(2) %>"
                                readonly="readonly" />
                          <% end %>
